// oxlint-disable-next-line sort-keys
const errorExtensionSymbols = {
  /** Symbol for the stack property of an error object. */
  STACK: 1,
  /** Symbol for the message property of an error object. */
  MESSAGE: 2,
  /** Symbol for the name property of an error object. */
  NAME: 3,
  /** Symbol for the cause property of an error object. */
  CAUSE: 4,
  /** Symbol for the constructor name property of an error object. */
  CONSTRUCTOR_NAME: 5,
  /** Symbol for the errors property of an error object. */
  ERRORS: 6,
  /** Symbol for the custom properties of an error object. */
  PROPERTIES: 7,
};

export default errorExtensionSymbols;
